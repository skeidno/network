# macOS

The macOS client is planned. Its UI and Network Extension implementation will live
in this directory and use the Apple libbox framework. No macOS runtime code is
shared with the Windows desktop process.
